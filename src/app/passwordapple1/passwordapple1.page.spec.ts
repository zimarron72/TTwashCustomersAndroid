import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Passwordapple1Page } from './passwordapple1.page';

describe('Passwordapple1Page', () => {
  let component: Passwordapple1Page;
  let fixture: ComponentFixture<Passwordapple1Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Passwordapple1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
